import { Overlay } from "@/components/cart/Overlay";

export default async function CartPage() {
	return (
		<Overlay>
			<h1 className="mb-4 text-3xl font-semibold">Cart Modal</h1>
		</Overlay>
	);
}
